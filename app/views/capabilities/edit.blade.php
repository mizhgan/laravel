@extends('layouts.scaffold')

@section('main')

<h1>Edit Capability</h1>
{{ Form::model($capability, array('method' => 'PATCH', 'route' => array('capabilities.update', $capability->id))) }}
	<ul>
        <li>
            {{ Form::label('name', 'Name:') }}
            {{ Form::text('name') }}
        </li>

		<li>
			{{ Form::submit('Update', array('class' => 'btn btn-info')) }}
			{{ link_to_route('capabilities.show', 'Cancel', $capability->id, array('class' => 'btn')) }}
		</li>
	</ul>
{{ Form::close() }}

@if ($errors->any())
	<ul>
		{{ implode('', $errors->all('<li class="error">:message</li>')) }}
	</ul>
@endif

@stop
